<?xml version="1.0" encoding="UTF-8"?>
<plugin>

  <condition>
    <min-version>${project.parent.version}</min-version>
  </condition>
  
  <information>
    <groupId>${project.groupId}</groupId>
    <artifactId>${project.artifactId}</artifactId>
    <version>${project.version}</version>
    <name>${project.name}</name>
    <description>${project.description}</description>
    <author>Sebastian Marsching</author>
    <url>${project.url}</url>
  </information>

  <packages>
    <package>com.aquenos.scm.ssh</package>
    <package>com.aquenos.scm.ssh.resource</package>
  </packages>

  <resources>
    <script>com/aquenos/scm/ssh/aquenos.scm.ssh.js</script>
  </resources>

</plugin>
